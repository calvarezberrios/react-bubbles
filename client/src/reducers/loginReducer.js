
const initialState = {
    colors: [],
    isFetching: false,
    error: ""
}

export default function colorsReducer(state = initialState, action) {
    switch(action.type) {
        default: return state;
    }
}